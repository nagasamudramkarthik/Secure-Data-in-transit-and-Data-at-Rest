apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-backend-disk
  namespace: dencrypt
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
  volumeName: pv-backend-disk
  storageClassName: manual
